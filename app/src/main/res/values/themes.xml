<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ExperimentV2" parent="Theme.MaterialComponents.Light.DarkActionBar">

        <!-- UI Elements color-->
        <item name="surface">@color/white</item>
        <item name="cardBackground">@color/cardview_light_background</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
         <item name="android:statusBarColor" tools:targetApi="l">@color/colorDarkSurface</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.ExperimentV2.Fullscreen" parent="Theme.ExperimentV2">
        <item name="android:actionBarStyle">@style/Widget.Theme.ExperimentV2.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.ExperimentV2.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>